const initialState = {
    popularMovies: {},
    isFetching: false,
    error: ''
}

export const getPopularMovies = (state = initialState, action) => {
    switch(action.type) {
        default :
            return state
    }
}