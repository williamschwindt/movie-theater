const initialState = {
    popularMovies: {},
    isFetching: false,
    error: ''
}

export const popularMoviesReducer = (state = initialState, action) => {
    switch(action.type) {
        default :
            return state
    }
}